import 'package:flutter/material.dart';

const mobileBackgroundColor = Color.fromRGBO(0, 0, 0, 1);
const webBackgroundColor = Color.fromRGBO(18, 18, 18, 1);
const mobileSearchColor = Color.fromRGBO(38, 38, 38, 1);
const blueColor = Color.fromRGBO(0, 149, 246, 1);
const primaryColor = Colors.white;
const secondaryColor = Colors.grey;
const LinearGradient purpleColor = LinearGradient(
  colors: [
    Color.fromRGBO(138, 43, 226, 1),   // Adjusted color with reduced brightness
    Color.fromRGBO(255, 105, 180, 1),  // Adjusted color with reduced brightness
  ],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);
const blueGradientColor = LinearGradient(
  colors: [
    Color(0xFF64B5F6), // Light Blue
    Color(0xFF0D47A1), // Dark Blue
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

